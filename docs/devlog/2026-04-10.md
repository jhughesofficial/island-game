# Devlog — 2026-04-10

## Shipped

- **Tutorial system** — TutorialManager autoload; 5 sequential hints persisted to `user://tutorial.json`
- **Guest count label** — "👥 N guests" bottom-left of island map
- **Peak income/s stat** — StatsPanel._process() tracks rolling peak
- **Enhanced ending screens** — tagline, run stats block (money/venues/upgrades/VIPs/secrets/time), entrance animation, Ghost Mode notice on retired ending
- **time_played tracking** — float seconds persisted to save, shown in ending stats
- **PI progress indicator** — "X / 100 PI" shown on status bar at 50-99 PI; gold tint at 100+
- **Tab keyboard shortcuts** — keys 1-4 switch Venues/Staff/Upgrades/VIPs tabs
- **Venue income display fix** — now correctly shows effective rate including quantity and upgrade multipliers (was showing raw base × count)
- **heat_suppress upgrade fix** — Legal Retainer (-0.015/s) and Hush Money (-0.030/s) now actually reduce heat_per_second; previously had no effect
- **Upgrade panel effect labels** — each upgrade row now shows "×2 bonfire", "×5 click", "▼ heat" etc.
- **Achievement fixes** — achievements persist across resets; all_venues condition now uses VenueData instead of dict size
- **3 new narrative events** — $42M "The Photograph", $75M "The Arrangement", $200M "The Exit"
- **TV overlay widget** — in-world TV screen on island map, 24 act-appropriate headlines cycling every 20s with blinking LIVE dot

## Design / Decisions

- Tutorial marks complete at $5K lifetime — past that point the player has enough momentum
- `secret_found` signal warning is benign — emitted from island_map.gd, acknowledged, no suppression needed
- heat_suppress upgrades: multiplier field repurposed to store heat/s reduction amount (0.015 / 0.030)
- Achievements persist like Ghost Mode — lifetime progress, not per-run state

## Next

1. **OGG audio files** — AudioManager is ready; need 3 act music tracks + 9 SFX (see `audio/README.md`)
2. **Venue sprites** — art pass with Recraft V4 Pro or Spritesheets.ai; current: themed emoji
3. Character creation — paper doll avatar (big feature, deferred)
4. Balance testing — needs actual play session to tune progression curve
