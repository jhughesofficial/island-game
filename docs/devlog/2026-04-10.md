# Devlog — 2026-04-10

## Shipped

- **Tutorial system** — TutorialManager autoload; 5 sequential hints (click→venue→heat→upgrade→vip) persisted to `user://tutorial.json`; hints dismissed on player action or after 8s; triggered from main.gd, island_map.gd, venue_panel.gd
- **Guest count label** — "👥 N guests" in bottom-left of island map, updates live on `venue_count_changed`
- **Peak income/s stat** — StatsPanel._process() tracks rolling peak; shown in Statistics panel as row 13

## Design / Decisions

- Tutorial marks complete at $5K lifetime — past that point the player has enough momentum to figure it out
- `secret_found` signal warning is benign — GDScript can't see emission from island_map.gd; acknowledged, no suppression needed

## Next

1. **OGG audio files** — AudioManager is ready; need 3 act music tracks + 9 SFX dropped into `audio/` (see `audio/README.md` for paths)
2. **Venue sprites** — art pass with Recraft V4 Pro or Spritesheets.ai; current state is themed emoji (functional)
3. Character creation — paper doll avatar (big feature, deferred)
4. TV news channel overlay — flavor escalation feature
